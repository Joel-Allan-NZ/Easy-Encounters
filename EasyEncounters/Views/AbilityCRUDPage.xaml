<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EasyEncounters.Views.AbilityCRUDPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasyEncounters.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:helpers="using:EasyEncounters.Helpers" xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToStringConverter x:Key="EnumToStringConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.Resources>
            <ResourceDictionary>
                <SolidColorBrush x:Key="DataGridCellFocusVisualPrimaryBrush" Color="Transparent"/>
                <SolidColorBrush x:Key="DataGridCellFocusVisualSecondaryBrush" Color="Transparent"/>
                <DataTemplate x:Key="RowDetailsTemplate">
                    <StackPanel>
                        <TextBlock Text="{Binding Ability.EffectDescription}"
                                   Margin="5"
                               TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </ResourceDictionary>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="*" MaxHeight="50" MinHeight="50"/>
        </Grid.RowDefinitions>
        <StackPanel Name="DataGridFilterBox"
                    Orientation="Horizontal"
                    Margin="0"
                    Grid.Row="0"
                    Background="{ThemeResource SystemAltHighColor}">
            <CommandBar DefaultLabelPosition="Right"
                            VerticalAlignment="Center"
                            Background="Transparent">
                <AppBarButton Icon="Filter"
                                  Label="Filter"
                                  AllowFocusOnInteraction="True"
                                  Width="80">
                    <AppBarButton.Flyout>
                        <Flyout>
                            <StackPanel Orientation="Horizontal">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="Spell Level:"/>
                                </StackPanel>
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <ComboBox ItemsSource="{x:Bind ViewModel.SpellLevels}"
                                                  SelectedItem="{x:Bind ViewModel.MinimumSpellLeveLFilter, Mode=TwoWay}"
                                                  MinWidth="124"
                                                  Margin="2">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Converter={StaticResource EnumToStringConverter}}"/>
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                        <TextBlock Text="-"
                                                       Margin="2"
                                                       VerticalAlignment="Center"/>
                                        <ComboBox ItemsSource="{x:Bind ViewModel.SpellLevels}"
                                                  SelectedItem="{x:Bind ViewModel.MaximumSpellLevelFilter, Mode=TwoWay}"
                                                  MinWidth="124"
                                                  Margin="2">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Converter={StaticResource EnumToStringConverter}}"/>
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Flyout>
                    </AppBarButton.Flyout>
                </AppBarButton>
            </CommandBar>
            <AutoSuggestBox x:Name="SearchBox"                            
                            HorizontalAlignment="Stretch"
                            MinWidth="200"
                            QueryIcon="Find"
                            VerticalAlignment="Center"
                            ItemsSource="{x:Bind ViewModel.SearchSuggestions, Mode=OneWay}"
                            TextMemberPath="Creature.Name"
                            DisplayMemberPath="Creature.Name"
                            FontSize="20"
                            Margin="5,0,0,0">
                <Interactivity:Interaction.Behaviors>
                    <Core:EventTriggerBehavior EventName="QuerySubmitted">
                        <Core:EventTriggerBehavior.Actions>
                            <Core:InvokeCommandAction Command="{x:Bind ViewModel.AbilityFilterCommand}" CommandParameter="{x:Bind SearchBox.Text, Mode=OneWay}"/>
                        </Core:EventTriggerBehavior.Actions>
                    </Core:EventTriggerBehavior>
                    <Core:EventTriggerBehavior EventName="TextChanged">
                        <Core:EventTriggerBehavior.Actions>
                            <Core:InvokeCommandAction Command="{x:Bind ViewModel.SearchTextChangeCommand}" CommandParameter="{x:Bind SearchBox.Text, Mode=OneWay}"/>
                        </Core:EventTriggerBehavior.Actions>
                    </Core:EventTriggerBehavior>
                </Interactivity:Interaction.Behaviors>
            </AutoSuggestBox>
            <CommandBar DefaultLabelPosition="Right"
                        Background="Transparent"
                        VerticalAlignment="Center"
                        Margin="0,0,8,0">
                <AppBarButton Label="Search"                      
                              Command="{x:Bind ViewModel.AbilityFilterCommand}"
                              CommandParameter="{x:Bind SearchBox.Text, Mode=OneWay}"/>
            </CommandBar>
        </StackPanel>
        <controls:DataGrid Grid.Row="1"
                           x:Name="AbilityListDG"
                           IsReadOnly="True"
                           SelectionMode="Single"
                           GridLinesVisibility="Horizontal"
                           RowDetailsTemplate="{StaticResource RowDetailsTemplate}"
                           Margin="0"
                           ItemsSource="{x:Bind ViewModel.Abilities}"
                           AutoGenerateColumns="False">
            <Interactivity:Interaction.Behaviors>
                <Core:EventTriggerBehavior EventName="Sorting">
                    <Core:InvokeCommandAction Command="{x:Bind ViewModel.DataGridSortCommand}"/>
                </Core:EventTriggerBehavior>
                <Core:EventTriggerBehavior EventName="DoubleTapped">
                    <Core:InvokeCommandAction Command="{x:Bind ViewModel.EditAbilityCommand}" CommandParameter="{x:Bind AbilityListDG.SelectedItem, Mode=OneWay}" />
                </Core:EventTriggerBehavior>
            </Interactivity:Interaction.Behaviors>
            <controls:DataGrid.RowStyle>
                <Style TargetType="controls:DataGridRow">
                    <Setter Property="controls:DataGridRow.ContextFlyout">
                        <Setter.Value>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Edit" Command="{Binding EditAbilityRequestedCommand}"/>
                                <MenuFlyoutItem Text="Copy" Command="{Binding CopyAbilityRequestedCommand}"/>
                                <MenuFlyoutItem Text="Delete" Command="{Binding DeleteAbilityRequestedCommand}"/>
                            </MenuFlyout>
                        </Setter.Value>
                    </Setter>
                </Style>
            </controls:DataGrid.RowStyle>
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Name"
                                             Tag="AbilityName"
                                             Binding="{Binding Ability.Name}"
                                             CanUserSort="True"/>
                <controls:DataGridTextColumn Header="Spell Level"
                                             Tag="AbilitySpellLevel"
                                             Binding="{Binding Ability.SpellLevel, Converter={StaticResource EnumToStringConverter}}"
                                             CanUserSort="True"/>
                <controls:DataGridTextColumn Header="Damage Type"
                                             Tag="AbilityDamageType"
                                             Binding="{Binding Ability.DamageTypes, Converter={StaticResource EnumToStringConverter}}"
                                             CanUserSort="True"/>
                <controls:DataGridTextColumn Header="Resolution Type"
                                             Tag="AbilityResolutionType"
                                             Binding="{Binding Ability.Resolution, Converter={StaticResource EnumToStringConverter}}"
                                             CanUserSort="True"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
        <Button Command="{x:Bind ViewModel.AddAbilityCommand}"
                Grid.Row="2"
                VerticalAlignment="Bottom"
                Margin="5"
                KeyboardAcceleratorPlacementMode="Auto"
                ToolTipService.ToolTip="Add a new Spell">
            <Button.KeyboardAccelerators>
                <KeyboardAccelerator Modifiers="Control" Key="N"/>
            </Button.KeyboardAccelerators>
            <FontIcon Glyph="&#xF8AA;"></FontIcon>
        </Button>
    </Grid>
</Page>
